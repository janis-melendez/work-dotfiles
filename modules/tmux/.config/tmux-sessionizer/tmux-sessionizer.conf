# Docs: https://github.com/ThePrimeagen/tmux-sessionizer/tree/master

# tmux-sessionizer config file example
# ------------------------
# # file: ~/.config/tmux-sessionizer/tmux-sessionizer.conf
# # If set this override the default TS_SEARCH_PATHS (~/ ~/personal ~/personal/dev/env/.config)
# TS_SEARCH_PATHS=(~/)
# # If set this add additional search paths to the default TS_SEARCH_PATHS
# # The number prefix is the depth for the Path [OPTIONAL]
# TS_EXTRA_SEARCH_PATHS=(~/ghq:3 ~/Git:3 ~/.config:2)
# # if set this override the TS_MAX_DEPTH (1)
# TS_MAX_DEPTH=2
# This is not meant to override .tmux-sessionizer.  At first i thought this
# would be a good command, but i don't think its ackshually what i want.
#
# Instead, its a list of commands to run on windows who's index is way outside
# of the first 10 windows.  This allows you to create as many windows in your
# session as you would like without having your workflow interrupted by these
# programatic windows
#
# how to use:
# tmux-sessionizer -w 0 will execute the first command in window -t 50.
# Set TS_COMMAND_WINDOW_BASE to override the default value of 50.
# TS_SESSION_COMMANDS=(<cmd1> <cmd2>)
#
# TS_LOG=true # will write logs to ~/.local/share/tmux-sessionizer/tmux-sessionizer.logs
# TS_LOG_FILE=<file> # will write logs to <file> Defaults to ~/.local/share/tmux-sessionizer/tmux-sessionizer.logs
# ------------------------

# Overrides the default value of 50.
TS_COMMAND_WINDOW_BASE=50

# Commands run by `tmux-sessionizer -s <index>`.
# Used for project-specific long-running tasks like dev servers, tests, or builds.
TS_SESSION_COMMANDS=(
    "codex"
    '[[ -f package.json ]] && npm run dev || zsh'
    '[[ -f package.json ]] && npm test -- --watch || zsh'
    '[[ -f docker-compose.yml || -f compose.yml ]] && docker compose up || zsh'
)

# Max directory depth to search for projects.
# Increase if projects are nested deeper, but higher values can make fzf slower/noisier.
TS_MAX_DEPTH=2

# Adds additional search paths to the default TS_SEARCH_PATHS
#   - Default TS_SEARCH_PATHS (~/ ~/personal ~/personal/dev/env/.config)
TS_EXTRA_SEARCH_PATHS=(
    "~",
)

# Enable logs (for debugging purposes)
# TS_LOG=file | echo # echo will echo to stdout, file will write to TS_LOG_FILE
# TS_LOG_FILE=<file> # will write logs to <file> Defaults to ~/.local/share/tmux-sessionizer/tmux-sessionizer.logs
